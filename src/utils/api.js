require('es6-promise').polyfill();
require('isomorphic-fetch');

export default class API {

  getHomeVideos(page){

    const fields = [

    ]

    return fetch()
  }

  getVideoInfo(id){

    const fields = [

    ]

    return fetch()
  }

}
